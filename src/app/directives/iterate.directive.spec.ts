import { IterateDirective } from './iterate.directive';

describe('IterateDirective', () => {
  it('should create an instance', () => {
    const directive = new IterateDirective();
    expect(directive).toBeTruthy();
  });
});
