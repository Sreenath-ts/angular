import { AdvancedPipePipe } from './advanced-pipe.pipe';

describe('AdvancedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AdvancedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
